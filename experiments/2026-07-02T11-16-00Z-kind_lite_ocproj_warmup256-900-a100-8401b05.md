# 2026-07-02T11-16-00Z-kind_lite_ocproj_warmup256-900-a100-8401b05

- method: kind_lite_ocproj_warmup256
- mode: 900-step gate on A100
- git_sha: 8401b057856fb32601c818198b121930a5f0959e
- machine: AIStation GPU2, NVIDIA A100-SXM4-80GB
- seed: 1337
- status: completed
- worktree: `/huyang2/muon_plus/_worktrees/kindlite_8401b05`
- actual_started_at: `2026-07-02T11:16:00Z`
- pid: `3500`
- pgid: `3500`
- launcher_log: `/huyang2/muon_plus/artifacts/2026-07-02T11-16-00Z-kind_lite_ocproj_warmup256-900-a100-8401b05.launcher.log`

## Hypothesis

The 300-step kind-lite result is the strongest early signal so far. The 900-step
gate tests whether module-specific preconditioner type is only an early
regularizer or whether it preserves the mid-run tail better than warmup-only
and Lite256.

## Configuration

- optimizer path: `newton_muon1`
- mode: `full`
- iterations: `900`
- validation cadence: every `300` steps
- validation tokens: `1048576`
- warmdown: disabled with `WARMDOWN_ITERS=0`
- flags:
  - `NEWMUON_TRUST=1`
  - `NEWMUON_TRUST_WARMUP_STEPS=256`
  - `NEWMUON_KIND_LITE_MASK=o,c_proj`
  - `NEWMUON_TELEMETRY_MAX_STEP=900`

## Command

```bash
RUN_ID=2026-07-02T11-16-00Z-kind_lite_ocproj_warmup256-900-a100-8401b05 \
SEED=1337 \
NUM_ITERATIONS=900 \
WARMDOWN_ITERS=0 \
VAL_LOSS_EVERY=300 \
VAL_TOKENS=1048576 \
SAVE_EVERY=0 \
NEWMUON_TRUST=1 \
NEWMUON_TRUST_WARMUP_STEPS=256 \
NEWMUON_KIND_LITE_MASK=o,c_proj \
NEWMUON_TELEMETRY_PATH=/huyang2/muon_plus/runs/${RUN_ID}/newton_telemetry.jsonl \
NEWMUON_TELEMETRY_MAX_STEP=900 \
./run.sh full newton_muon1
```

## Results

- step 300 val_loss: `4.3656`
- step 600 val_loss: `3.9164`
- step 900 val_loss: `3.7746`
- final_val_loss: `3.7746`
- train_time_s: `2220.484`
- peak_memory: `39450 MiB`

## Decision

Keep if it beats warmup-only 900 (`3.7770`) and current-code full Trust 900
(`3.7777`). Strong signal requires approaching or beating Lite256 900
(`3.7762`) while retaining the much better 300-step point.

Step 300 is still positive but less sharp than the standalone 300 gate:
`4.3656` versus `4.3643`. It remains ahead of warmup-only (`4.3665`) and
Lite256 (`4.3667`), and is within the prior Lite+Adaptive 300 region. Continue
to 600 to see whether the mixed preconditioner improves the mid-run tail.

Step 600 weakens the case: `3.9164` still beats full Trust (`3.9177`), but loses
to warmup-only (`3.9151`) and the A100 Lite256 mirror (`3.9144`). Continue to
900 as the final gate only; do not extend to 1200 unless the final point clearly
reverses this mid-run weakness.

Final 900 reverses the mid-run weakness: `3.7746` beats current-code full Trust
(`3.7777`), warmup-only (`3.7770`), and Lite256 (`3.7762`). Decision: keep as the
current positive mechanism and promote exactly one 1200 gate. The insight is
structural rather than scalar: the low-trust `o,c_proj` families appear to need
diagonal activation scaling, while the remaining modules still benefit from full
activation covariance.
