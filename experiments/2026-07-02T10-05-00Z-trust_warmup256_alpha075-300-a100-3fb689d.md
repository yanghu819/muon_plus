# 2026-07-02T10-05-00Z-trust_warmup256_alpha075-300-a100-3fb689d

- method: trust_warmup256_alpha075
- mode: 300-step gate on A100
- git_sha: 3fb689df25faec35cce414fdb494150a362d4caa
- machine: AIStation GPU2, NVIDIA A100-SXM4-80GB
- seed: 1337
- status: in_progress
- worktree: `/huyang2/muon_plus/_worktrees/hybrid_3fb689d`
- actual_started_at: `2026-07-02T10:06:28Z`
- pid: `6982`
- pgid: `6982`
- launcher_log: `/huyang2/muon_plus/artifacts/2026-07-02T10-05-00Z-trust_warmup256_alpha075-300-a100-3fb689d.launcher.log`

## Hypothesis

Warmup256 reaches a clean early gain and still beats full Trust at 900, but it
falls short of Lite256 by `0.0008`. That pattern suggests alpha `1.0` after the
warmup may over-trust the activation-side inverse slightly. A single capped
run with `alpha_max=0.75` tests whether partial Newton strength keeps the
directional correction while reducing late over-preconditioning.

## Configuration

- optimizer path: `newton_muon1`
- mode: `full`
- iterations: `300`
- validation cadence: every `300` steps
- validation tokens: `1048576`
- warmdown: disabled with `WARMDOWN_ITERS=0`
- flags:
  - `NEWMUON_TRUST=1`
  - `NEWMUON_TRUST_WARMUP_STEPS=256`
  - `NEWMUON_TRUST_ALPHA_MAX=0.75`
  - `NEWMUON_TELEMETRY_MAX_STEP=300`

## Command

```bash
RUN_ID=2026-07-02T10-05-00Z-trust_warmup256_alpha075-300-a100-3fb689d \
SEED=1337 \
NUM_ITERATIONS=300 \
WARMDOWN_ITERS=0 \
VAL_LOSS_EVERY=300 \
VAL_TOKENS=1048576 \
SAVE_EVERY=0 \
NEWMUON_TRUST=1 \
NEWMUON_TRUST_WARMUP_STEPS=256 \
NEWMUON_TRUST_ALPHA_MAX=0.75 \
NEWMUON_TELEMETRY_PATH=/huyang2/muon_plus/runs/${RUN_ID}/newton_telemetry.jsonl \
NEWMUON_TELEMETRY_MAX_STEP=300 \
./run.sh full newton_muon1
```

## Results

- pending

## Decision

This is not an alpha sweep. It is one mechanism check. If step 300 is worse
than uncapped warmup (`4.3663/4.3665`) by more than small noise, alpha capping
is harming the early gain and should be discarded. If it matches or improves,
promote one 900-step gate to test whether capping fixes the late miss.
