# 2026-07-02T04-27-30Z-lite256_full_trust-1200-3fb689d

- method: lite256_full_trust
- mode: 1200-step extension gate
- git_sha: 3fb689df25faec35cce414fdb494150a362d4caa
- machine: AIStation GPU2, NVIDIA A800-SXM4-80GB
- seed: 1337
- status: in_progress
- worktree: `/huyang2/muon_plus/_worktrees/hybrid_3fb689d`

## Hypothesis

Lite256->full Trust is the only temporal schedule with a positive 900-step
signal so far: it beats current full Trust and Lite+Adaptive, while later switch
and adaptive gating both failed at 300. Before spending a full 6200-step budget,
run a modest 1200-step extension to see whether the 900-step edge persists or
collapses once the schedule moves further into the full-covariance tail.

## Configuration

- optimizer path: `newton_muon1`
- mode: `full`
- iterations: `1200`
- validation cadence: every `300` steps
- validation tokens: `1048576`
- warmdown: disabled with `WARMDOWN_ITERS=0`
- flags:
  - `NEWMUON_LITE_UNTIL_STEP=256`
  - `NEWMUON_TRUST=1`
  - `NEWMUON_TELEMETRY_MAX_STEP=1200`

## Command

```bash
RUN_ID=2026-07-02T04-27-30Z-lite256_full_trust-1200-3fb689d \
SEED=1337 \
NUM_ITERATIONS=1200 \
WARMDOWN_ITERS=0 \
VAL_LOSS_EVERY=300 \
VAL_TOKENS=1048576 \
SAVE_EVERY=0 \
NEWMUON_TRUST=1 \
NEWMUON_LITE_UNTIL_STEP=256 \
NEWMUON_TELEMETRY_PATH=/huyang2/muon_plus/runs/${RUN_ID}/newton_telemetry.jsonl \
NEWMUON_TELEMETRY_MAX_STEP=1200 \
./run.sh full newton_muon1
```

## Results

- step 300 val_loss: `4.3690`
- step 600 val_loss: `3.9162`
- step 900 val_loss: `3.7767`

## Decision

Step 900 passes the prune gate, but only narrowly. The step-300 point matched
the prior lite256 900-step run (`4.3689`) rather than the standalone 300 gate
(`4.3667`), so it did not change the early-curve picture. The step-600 point
was ahead of the prior lite256 run (`3.9176`), current full Trust (`3.9184`),
and Lite+Adaptive (`3.9193`), which supported the idea that the Lite warm start
could improve the mid-run full-covariance tail. At step 900 the run still beats
current fixed-seed full Trust (`3.7777`) but is slightly worse than the previous
lite256 900-step run (`3.7762`). Continue to step 1200, because the remaining
question is stability rather than early superiority: if the final point stays
ahead of full Trust, promote a single full-length lite256 run; otherwise stop
this temporal schedule and mine telemetry before changing algorithms again.
