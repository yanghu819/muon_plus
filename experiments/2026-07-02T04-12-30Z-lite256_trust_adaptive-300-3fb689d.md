# 2026-07-02T04-12-30Z-lite256_trust_adaptive-300-3fb689d

- method: lite256_trust_adaptive
- mode: 300-step gate
- git_sha: 3fb689df25faec35cce414fdb494150a362d4caa
- machine: AIStation GPU2, NVIDIA A800-SXM4-80GB
- seed: 1337
- status: completed
- worktree: `/huyang2/muon_plus/_worktrees/hybrid_3fb689d`

## Hypothesis

Lite+Adaptive produced the best early curve, while full Trust produced the
better mid/late curve. Static full-covariance adaptive was bad, but the
temporal schedule changes the question: use Lite early, switch at 256, and let
adaptive gating damp only unreliable preconditioned directions during the tail.
If this helps, it should match or beat lite256 at the 300 gate without losing
the mechanism that improved 600/900.

## Configuration

- optimizer path: `newton_muon1`
- mode: `full`
- iterations: `300`
- validation cadence: every `300` steps
- validation tokens: `1048576`
- warmdown: disabled with `WARMDOWN_ITERS=0`
- flags:
  - `NEWMUON_LITE_UNTIL_STEP=256`
  - `NEWMUON_TRUST=1`
  - `NEWMUON_LAYER_ADAPTIVE=1`
  - `NEWMUON_ADAPTIVE_NORM_MAX=16`
  - `NEWMUON_TELEMETRY_MAX_STEP=300`

## Command

```bash
RUN_ID=2026-07-02T04-12-30Z-lite256_trust_adaptive-300-3fb689d \
SEED=1337 \
NUM_ITERATIONS=300 \
WARMDOWN_ITERS=0 \
VAL_LOSS_EVERY=300 \
VAL_TOKENS=1048576 \
SAVE_EVERY=0 \
NEWMUON_TRUST=1 \
NEWMUON_LAYER_ADAPTIVE=1 \
NEWMUON_ADAPTIVE_NORM_MAX=16 \
NEWMUON_LITE_UNTIL_STEP=256 \
NEWMUON_TELEMETRY_PATH=/huyang2/muon_plus/runs/${RUN_ID}/newton_telemetry.jsonl \
NEWMUON_TELEMETRY_MAX_STEP=300 \
./run.sh full newton_muon1
```

## Results

- step 300 val_loss: `4.3783`
- train_time_s: `687.962`
- peak_memory_mib: `39446`

## Decision

Discard. The adaptive gate badly underperforms the plain lite256 temporal
schedule (`4.3667`) and even loses to fixed-seed full Trust's later 300 anchor
gap less decisively than needed. Combining Trust and LayerAdaptive appears too
conservative or noisy in this temporal setup. Keep the simpler lite256 full
Trust schedule as the current positive mechanism.
