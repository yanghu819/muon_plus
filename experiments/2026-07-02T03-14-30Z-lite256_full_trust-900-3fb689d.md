# 2026-07-02T03-14-30Z-lite256_full_trust-900-3fb689d

- method: lite256_full_trust
- mode: 900-step promotion gate
- git_sha: 3fb689df25faec35cce414fdb494150a362d4caa
- machine: AIStation GPU2, NVIDIA A800-SXM4-80GB
- seed: 1337
- status: completed
- worktree: `/huyang2/muon_plus/_worktrees/hybrid_3fb689d`
- run_dir: `/huyang2/muon_plus/runs/2026-07-02T03-14-30Z-lite256_full_trust-900-3fb689d`
- launcher_log: `/huyang2/muon_plus/artifacts/2026-07-02T03-14-30Z-lite256_full_trust-900-3fb689d.launcher.log`

## Hypothesis

The 300-step hybrid gate finished at `4.3667`, close to the best Lite+Adaptive
early values and better than Lite+Trust. The full Trust anchor later beat
Lite+Adaptive by step 900. If the temporal split is real, this 900-step run
should keep Lite-like early speed and then beat both static Lite+Adaptive
(`3.7817`) and current full Trust (`3.7777`) by the final gate.

## Configuration

- optimizer path: `newton_muon1`
- mode: `full`
- iterations: `900`
- validation cadence: every `300` steps
- validation tokens: `1048576`
- warmdown: disabled with `WARMDOWN_ITERS=0`
- flags:
  - `NEWMUON_TRUST=1`
  - `NEWMUON_LITE_UNTIL_STEP=256`
  - `NEWMUON_TELEMETRY_MAX_STEP=900`

## Command

```bash
RUN_ID=2026-07-02T03-14-30Z-lite256_full_trust-900-3fb689d \
SEED=1337 \
NUM_ITERATIONS=900 \
WARMDOWN_ITERS=0 \
VAL_LOSS_EVERY=300 \
VAL_TOKENS=1048576 \
SAVE_EVERY=0 \
NEWMUON_TRUST=1 \
NEWMUON_LITE_UNTIL_STEP=256 \
NEWMUON_TELEMETRY_PATH=/huyang2/muon_plus/runs/${RUN_ID}/newton_telemetry.jsonl \
NEWMUON_TELEMETRY_MAX_STEP=900 \
./run.sh full newton_muon1
```

## Results

- step 300 val_loss: `4.3689`
- step 600 val_loss: `3.9176`
- step 900 val_loss: `3.7762`
- train_time_s: `2233.653`
- peak_memory_mib: `39446`

## Decision

Continue to step 600. The 300-step point is weaker than the standalone
Lite256->full 300 gate (`4.3667`) and slightly weaker than Lite+Trust
(`4.3677`), but it is still much better than the fixed-seed full Trust 300
anchor (`4.3810`). The core question is not whether the schedule is the single
best early curve; it is whether keeping early speed close enough lets the
full-covariance tail win by 600/900.

Interim update at step 600: the schedule is now ahead of both fixed-seed full
Trust (`3.9184`) and Lite+Adaptive (`3.9193`) at the same gate. The margin is
small, but the direction is exactly what the temporal hypothesis predicts:
after a weaker 300-step point, the full-covariance tail starts paying back.

Final update at step 900: `3.7762`. This beats the current fixed-seed full
Trust anchor (`3.7777`) and Lite+Adaptive (`3.7817`), but does not beat the
older full Trust reference (`3.7713`). Keep the mechanism: temporal scheduling
works. Do not overclaim the result as a new overall best. Next decision is to
move the switch later, because the 300-step point was weaker than desired while
the 600/900 tail was positive.
